pipelines:
  - meteo.modbus > |meteo.divide| > |meteo.round_wind_direction| > |meteo.duplicate| > |meteo.format| > meteo.redis.cache
  - test.data > |meteo.divide| > |meteo.round_wind_direction| > |meteo.duplicate| > |meteo.format| > meteo.redis.cache

mocks:
  - name: test.data
    json_file_path: /home/alexej/repos/redis-adapter/mocks/data.json

redis: !include redis.yaml
interceptors: !include interceptors.yaml
modbus: !include modbus.yaml
validators: !include validators.yaml
